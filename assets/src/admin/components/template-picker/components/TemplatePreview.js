const TemplatePreview = ( { templateId } ) => {
	return (
		<div className="srfm-ts-main-container" style={ { marginTop: '5rem' } }>
			{ templateId }
		</div>
	);
};

export default TemplatePreview;
