/*
 * Selectors for the SureForms store
 */

/**
 * Select the global data from the store.
 *
 * @param {Object} state - The current state of the store
 */
export const selectSrfmGlobalData = ( state ) => state.srfmGlobalData;
